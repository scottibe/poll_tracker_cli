require 'spec_helper'

describe PollTracker do
  it 'has a version number' do
    expect(PollTracker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
